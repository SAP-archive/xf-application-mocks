apiVersion: apps/v1
kind: Deployment
metadata:
  name: xf-mock
  labels:
    app: xf-mock
spec:
  selector:
    matchLabels:
      app: xf-mock
  replicas: 1
  template:
    metadata:
      labels:
        app: xf-mock
    spec:
      containers:
      - image: eu.gcr.io/kyma-project/xf-application-mocks/xf-mock:latest
        imagePullPolicy: Always
        name: xf-mock
        ports:
        - name: http
          containerPort: 10000
        env:
        - name: DEBUG
          value: "true"
        resources:
          requests:
            memory: "150Mi"
            cpu: "50m"
          limits:
            memory: "250Mi"
            cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: xf-mock
  labels:
    app: xf-mock
spec:
  ports:
  - name: http
    port: 10000
  selector:
    app: xf-mock
